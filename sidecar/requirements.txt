# Backend (sidecar) dependencies.
#
# torch: install the build that matches the target machine BEFORE this file.
#   CUDA dev/target machine:
#     pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#   CPU-only machine (current dev box has no CUDA device):
#     pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
# Pinning it here would force one of the two on everybody, so it is left out.

# --- web / process ---
fastapi==0.115.12
uvicorn[standard]==0.34.0
pydantic==2.10.6
pydantic-settings==2.7.1
python-multipart==0.0.20

# --- persistence ---
SQLAlchemy==2.0.36
alembic==1.14.1
qdrant-client==1.12.2

# --- document & image ingestion ---
PyMuPDF==1.25.4
python-docx==1.1.2
pillow==10.4.0

# --- ML ---
sentence-transformers==3.4.1
open_clip_torch==3.3.0
transformers==4.53.1
accelerate==1.3.0
timm>=1.0.17
einops==0.8.0
pyvips-binary==8.16.0; sys_platform == "win32"   # moondream2 image preprocessing
pytesseract==0.3.13

# --- utility ---
huggingface-hub==0.30.2
numpy<2.3
